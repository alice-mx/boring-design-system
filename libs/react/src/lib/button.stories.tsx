import React from 'react';
import { Button } from './button';

export default {
  component: React,
  title: 'React',
};

export const primary = () => {
  return <Button text="I am the button" />;
};
